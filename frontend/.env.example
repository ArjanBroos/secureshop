# Frontend environment variables
# Copy to .env.local for local development — never commit .env.local

# The base URL of the backend API
# Local dev: leave empty (Vite proxy handles /actuator/* → localhost:8080)
# Staging/prod: set at build time by the CI pipeline
VITE_API_URL=
